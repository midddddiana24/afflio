import type { NextApiRequest, NextApiResponse } from "next";
export default function handler(req: NextApiRequest, res: NextApiResponse) { if (req.method !== "GET") return res.status(405).json({ error: "Method not allowed." }); const required = ["NEXT_PUBLIC_SUPABASE_URL", "NEXT_PUBLIC_SUPABASE_ANON_KEY", "SUPABASE_SERVICE_ROLE_KEY"]; const missing = required.filter((name) => !process.env[name]); return res.status(missing.length ? 503 : 200).json({ status: missing.length ? "degraded" : "ok", version: process.env.VERCEL_GIT_COMMIT_SHA?.slice(0, 7) || "local", checks: { databaseConfig: !missing.length, paymentConfig: Boolean(process.env.PAYMENT_PROVIDER), queueConfig: Boolean(process.env.CLICK_QUEUE_SECRET), monitoringConfig: Boolean(process.env.SENTRY_DSN) } }); }
